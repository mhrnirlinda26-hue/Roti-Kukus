import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rotikukus.thailand',
  appName: 'Roti Kukus Thailand',
  webDir: 'out',
  server: {
    androidScheme: 'https',
  },
};

export default config;
